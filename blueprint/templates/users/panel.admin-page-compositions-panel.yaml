kind: Panel
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: admin-page-users-panel
  namespace: {{ .Release.Namespace }}
spec: 
  widgetData:
    actions: {}
    title: Local Users, Groups, and Permissions
    items:
      - resourceRefId: column
  resourcesRefs:
    items:
    - id: column
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: admin-page-users-panel-column
      namespace: {{ .Release.Namespace }}
      resource: columns
      verb: GET