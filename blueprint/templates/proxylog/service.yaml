apiVersion: v1
kind: Service
metadata:
  name: {{ include "proxylog.fullname" . }}
  labels:
    {{- include "proxylog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "proxylog.selectorLabels" . | nindent 4 }}
